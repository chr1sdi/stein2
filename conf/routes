# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~

# Home page
GET     /                           controllers.Application.index

# Question Actions
GET     /question/new               controllers.Question.propose
POST    /question/new               controllers.Question.submit
GET     /question/unapproved        controllers.Question.unapproved
POST    /question/approve           controllers.Question.approve

# Map static resources from the /public folder to the /assets URL path
GET     /assets/*file               controllers.Assets.at(path="/public", file)
